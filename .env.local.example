# URL of the FIRE-X FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional map tile overrides (empty = free Carto dark + Esri satellite defaults)
NEXT_PUBLIC_MAP_TILE_URL=
NEXT_PUBLIC_SATELLITE_TILE_URL=